#include <iostream>
using namespace std;

void Greedy(int arr[], int n)
{
    int tamp[n];
}

int main()
{
    int n;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }

    Greedy(arr, n);
}

// TODO: Not working